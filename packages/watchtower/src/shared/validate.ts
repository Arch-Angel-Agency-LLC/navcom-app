/**
 * Moved to @navcom/core, where every consumer can reach it — a client parsing a response
 * and a terminal reading a config need the same guarantees the daemon needed.
 */
export * from "@navcom/core";
