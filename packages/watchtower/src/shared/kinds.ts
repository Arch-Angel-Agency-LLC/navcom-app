/**
 * Moved to @navcom/core. Re-exported so existing imports keep working.
 *
 * The daemon and the clients drifted apart precisely here — same four numbers, two
 * definitions — so there is now one.
 */
export { KIND_WATCH_STATE, KIND_SIGNAL, KIND_DISTRESS, KIND_RESPONSE } from "@navcom/core";
